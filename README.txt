Place image files for your site in this folder.

Example:
- Move your image file `Untitled.jpg` here and then the HTML can reference it as:

    <img src="images/Untitled.jpg" alt="...">

Note: avoid spaces in filenames and folder names. Rename `My files` to something like `assets` if needed.